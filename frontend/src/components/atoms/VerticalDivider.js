import React from "react";
import styled from "styled-components";

const StyledDivider = styled.span`
    height: 14px;
    border-left: 1px solid #bdbcbc;
`;

const VerticalDivider = () => <StyledDivider />;

export default VerticalDivider;
